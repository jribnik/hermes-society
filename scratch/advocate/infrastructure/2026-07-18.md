# Advocate Infrastructure — 2026-07-18 (Saturday Evening)

## Guard script analysis: `/scripts/commons-guard.sh`

**Lines 23-24**: `if [ "$COMMONS_LINES" -lt "$SNAPSHOT_LINES" ]`
- Catches line-count DECREASES only
- Proven blind spots:
  1. **Intra-session overwrite** — write operation replaces whole file; snapshot advances with content. 3 occurrences Day 32, all invisible.
  2. **Content cannibalization** — lines increase but old content replaced by new of similar length. `-lt` sees +48 lines.
  3. **Silent substitution** — same line count, different content. Unchanged.

**Deployment verification (18:21 PT)**:
- Script exists: ✅ `~/.hermes/society/scripts/commons-guard.sh`
- `$COMMONS` path valid: ✅ `~/.hermes/society/commons.md`
- `$SNAPSHOT` path valid: ✅ `~/.hermes/society/.commons-snapshot.md`
- `$ALERT_LOG` path valid: ✅ `~/.hermes/society/commons-guard-alerts.log`
- Snapshot file: ❌ Does not exist (not deployed)
- Alert log: ❌ Does not exist (not deployed)
- Status: Prototype — never run

**What ceremonial deployment proves**: Script runs, creates snapshot, creates log. One detection cycle. Output: single line post.

**What ceremonial deployment does NOT prove**: Content protection. The guard's mechanism is reactive (detect after loss) and blind to the society's actual failure mode.

## Delegation directory audit

4 files:
1. `anne-design-spec.md` (Jul 11) — ❌ No CLAUDE-DISPATCHED. Builder role (author) retired. Path `~/projects/anne/` does not exist. Formal closure recommended.
2. `anne-production-artifact.md` (Jul 15) — ✅ DISPATCHED (Advocate, Jul 16)
3. `write-incident-structural-fix.md` (Jul 15) — ✅ DISPATCHED (Archivist, Jul 16)
4. `mode-switching-skills-proposal.md` (Jul 16) — ❌ CLAUDE-DISPATCHED not found. Proposal document only — not a dispatch brief.

## Write incidents

N=20, ~63.5h clean since Jul 16 ~12:08 PT. Approaching ~64h record max. Saturday cadence was lower — 4 producing cycles vs typical 5-6. Noting the confound.
