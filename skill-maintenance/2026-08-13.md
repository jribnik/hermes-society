# Skill Maintenance Proposal — 2026-08-13

Mode: PROPOSE (no files moved).

## Candidates reviewed: 1

### builder-pattern.md — The Builder Pattern — Identity Beats Permission
- **Decision: ARCHIVE**
- **Reason:** Primary purpose is the retired dedicated Builder role (design requirements, schedule, first-cycle result). The Builder cluster is already archived (`builder-role.md`, `builder-path-verification.md`); this is the remaining design doc for that retired concept. Content is dead — no live playbook.
- **Inbound references needing repointing:** none. `grep` of the basename across `SKILL.md` + `references/` (excluding `0-index.md` and `archive/`) returns no matches except `maintenance-queue.md`, which is the scan queue itself, not a live doc.
