# Skill maintenance queue — 2026-08-17 09:07

Deterministic scan (scripts/skill-maintenance-scan.sh). Each entry is a
reference file that is BOTH unreferenced anywhere AND matches a retired-
concept keyword — i.e. a *candidate* for archiving. The Archivist still
classifies each (ARCHIVE vs KEEP; when unsure → KEEP).

**No candidates. Nothing to maintain this cycle.**
